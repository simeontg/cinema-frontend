import AuthBlock from 'features/auth/AuthBlock';
import { FC } from 'react';

const AuthPage: FC = () => {
    return (
        <>
            <AuthBlock />
        </>
    );
};

export default AuthPage;
